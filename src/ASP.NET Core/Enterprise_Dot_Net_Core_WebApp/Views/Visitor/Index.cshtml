@model Tuple<
        IEnumerable<Enterprise_Dot_Net_Core_WebApp.Core.Entities.Enterprise_MVC_Core>,
        IEnumerable<Enterprise_Dot_Net_Core_WebApp.Core.Entities.Enterprise_MVC_Core>,
        IEnumerable<Enterprise_Dot_Net_Core_WebApp.Core.Entities.Enterprise_MVC_Core>
        >


@{
    ViewData["Title"] = "Index";
}

<h1>Visitor - Index</h1>

<h4>Visitor 1</h4>
<h6>Step1 - Select all of data.</h6>
<table class=" table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Item1.FirstOrDefault().ID)</th>
            <th>@Html.DisplayNameFor(m => m.Item1.FirstOrDefault().Name)</th>
            <th>@Html.DisplayNameFor(m => m.Item1.FirstOrDefault().Age)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Item1)
        {
            <tr>
                <td>@Html.DisplayFor(i => item.ID)</td>
                <td>@Html.DisplayFor(i => item.Name)</td>
                <td>@Html.DisplayFor(i => item.Age)</td>
            </tr>
        }
    </tbody>
</table>
<h6>Step 2 - Select last data.</h6>
<table class=" table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Item2.FirstOrDefault().ID)</th>
            <th>@Html.DisplayNameFor(m => m.Item2.FirstOrDefault().Name)</th>
            <th>@Html.DisplayNameFor(m => m.Item2.FirstOrDefault().Age)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Item2)
        {
            <tr>
                <td>@Html.DisplayFor(i => item.ID)</td>
                <td>@Html.DisplayFor(i => item.Name)</td>
                <td>@Html.DisplayFor(i => item.Age)</td>
            </tr>
        }
    </tbody>
</table>
<hr />
<h4>Visitor 2</h4>
<h6>Step 1 - Select last data.</h6>
<table class=" table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Item3.FirstOrDefault().ID)</th>
            <th>@Html.DisplayNameFor(m => m.Item3.FirstOrDefault().Name)</th>
            <th>@Html.DisplayNameFor(m => m.Item3.FirstOrDefault().Age)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Item3)
        {
            <tr>
                <td>@Html.DisplayFor(i => item.ID)</td>
                <td>@Html.DisplayFor(i => item.Name)</td>
                <td>@Html.DisplayFor(i => item.Age)</td>
            </tr>
        }
    </tbody>
</table>


